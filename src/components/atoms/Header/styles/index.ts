/**
 *
 * Header Styles
 *
 */

import {StyleSheet} from 'react-native';

const HeaderStyles = StyleSheet.create({
  container: {
    paddingVertical: 30,
  },
  label: {
    fontWeight: 'bold',
    fontSize: 30,
  },
});

export default HeaderStyles;
